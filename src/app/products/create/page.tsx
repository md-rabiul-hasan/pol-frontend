// Import the ProductCreateForm component
import ProductCreateForm from './Form';

/**
 * ProductCreatePage Component
 * Renders the ProductCreateForm to allow users to create a new product.
 */
const ProductCreatePage = () => {
  return <ProductCreateForm />;
};

export default ProductCreatePage;
